
SELECT "SID", "CAT"
FROM "RESULTS"
WHERE "SID" = 103;

SELECT "ENO", "POINTS"
FROM "RESULTS"
WHERE "POINTS" >= 10;

SELECT "SID", "CAT"
FROM "RESULTS"
WHERE "SID" = 103
UNION ALL
SELECT "ENO", "POINTS"
FROM "RESULTS"
WHERE "POINTS" >= 10;


SELECT "SID", "CAT"
FROM "RESULTS"
WHERE "SID" = 103
UNION ALL
SELECT "POINTS", "CAT"
FROM "RESULTS"
WHERE "POINTS" >= 10;

