这个任务的目标是学习训练和使用生成模型来完成图像相关的任务。
我们为你提供了一个Jupyter笔记本，说明了你如何建立、训练和使用变异自动编码器（VAE）和生成对抗网络（GAN）。
你可以在这里下载使用的数据集。
你在这个任务中的主要目标是找到你自己的图像数据集，对它进行预处理，重新训练这些网络，并使用这些模型来生成新的内容。

最先进的生成模型可以为它们所训练的任何数据创造出无穷无尽的逼真图像（例如StyleGAN2）。
然而，这种模型的训练是非常困难和昂贵的，因此最好从小处着手，了解更小更简单的架构背后的工作原理。

1. 通过所提供的笔记本，尝试了解生成模型是如何使用Keras构建的。
首先，我们为你提供了一个卷积自动编码器（CAE）架构，它包含两个主要的构建模块--编码器和解码器。
VAE的构建方式非常相似，但包括一个采样层，使这个模型具有生成能力。
GAN模型中的生成器和判别器也是使用这些类似的构件建立的，但是它们的排列方式不同，使用的损失函数也非常不同。

2. 用多个类别或单一类别中高度不同的图像来训练 "普通 "的生成模型，可能会导致模型不能产生有趣的输出。
这就是为什么人脸数据集经常被用来展示生成模型的力量，因为它们通常非常统一（人们以类似的距离和角度面对摄像机）。
然而，肯定还有许多其他有趣和可行的用例和应用。
找到一个你选择的新图像数据集，并使用笔记本中提供的代码重新训练你的生成模型（包括VAE和GAN）。
你应该在报告中提供一个你所使用的数据的链接以及它的描述。

3. 为了正确地训练模型，你可能需要重新调整你的数据，使其接近于示例数据集的大小。
由于笔记本中的所有神经网络都是用函数build conv net()和build deconv net()构建的，如果你愿意，你也可以调整卷积架构--既能更接近你的数据规模，又能减少/增加架构的复杂性。

4. 一旦你重新训练了模型，在提交的报告中包括修改后的笔记本和你的结果。
为你的生成模型的输出创建一些说明性的表述。

5. 一旦你的模型被训练好了，它们的潜在空间就会为它们所训练的数据的分布建模。
你可以使用随机生成的向量作为这些模型的解码器组件的输入，以生成新的图像（VAE中的解码器，GAN中的生成器）。
每个向量代表这个潜在空间中的一个点，由于这个空间是连续的，你可以产生各种有趣的可视化。
你的目标是通过在潜空间的两个随机点之间进行插值来创造其中的一个视觉效果。
你能想到潜数据表示法的其他创造性用途吗？

在你的报告中详细描述这些生成模型的工作原理，并强调它们之间的差异。
